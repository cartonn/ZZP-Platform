# next-auth (@auth/core) lijst nodemailer als peerOptional ^7, terwijl wij nodemailer ^8
# draaien. Dat is een optionele peer en compatibel; zonder deze regel breekt `npm ci` met
# ERESOLVE in CI. Dit maakt npm ci (CI), npm install (Docker) en lokaal gedrag identiek.
legacy-peer-deps=true
