# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# typescript build info
*.tsbuildinfo

# production
/build

# testing
/coverage
/test-results
/playwright-report
/blob-report
/.playwright
/e2e/screenshots/
/e2e/qa/screenshots/

# env
.env
.env*.local

# Claude Code — persoonlijke, lokale settings/permissies (niet delen met het team)
.claude/settings.local.json

# database (local sqlite)
*.db
*.db-journal
/prisma/*.db
/prisma/*.db-journal

# uploaded documents (never commit private docs)
/storage/
/uploads/

# misc
.DS_Store
*.pem
npm-debug.log*
.vscode/*
!.vscode/extensions.json
.idea

# Lokale Pidz-observatie-screenshots — bevatten persoonsgegevens van derden, nooit committen (AVG).
pidz/

# Persona-sweep screenshots (zelf-test-lus) — lokaal/CI-artefact, niet committen
e2e/personas/shots/

# Stray design-mockups dropped in repo root (not part of the app)
zzp-platform-ontwerpen.html
*.local.html
agent-review-verdict.txt
