# V5 homepage and platform

The owner asked to use the existing Handslag V5 design as the production landing page as well as the platform. `/` now renders the native React version of that design: the original two logo paths open around “Een goede opdracht begint bij handslag.”, words appear in sequence, and the audience documents retain their responsive scroll/depth behavior. Styles are scoped to the landing page; navigation goes to the same application's login, registration and legal pages. The dashboard remains protected.

The existing preview photograph (Age Cymru / Unsplash, 1800 × 1273) is unchanged. It is served from the owner-controlled Handslag V5 Railway preview at `/assets/hero-samenwerken.jpg`, with an exact Next Image remote-path allowlist. The preview service now explicitly serves that asset; it must remain available for uncached image optimization. Existing preview HTML is unchanged. Open Sans is self-hosted in this repository; font provenance is recorded alongside the font.

Local validation: lint, TypeScript, all 8,692 unit tests (2 existing skips), formatting, and three Playwright landing journeys passed. The journeys cover registration and protected-dashboard redirection, all three audiences, keyboard FAQ interaction, mobile overflow, and completed quote placement after resize. Desktop and 390 × 844 mobile views were visually checked. A fresh production build and all six required GitHub checks are required before merge.
